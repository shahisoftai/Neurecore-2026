/**
 * Table components barrel export (Phase 8)
 *
 * SOLID: Single export point for data display components
 * Maintains clean import paths: import { UnifiedDataTable } from '@/components/table'
 */

export {
    UnifiedDataTable,
    type ColumnDef,
    type PaginationConfig,
    type BulkAction,
    type UnifiedDataTableProps,
} from './UnifiedDataTable';
